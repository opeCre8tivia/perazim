import React from "react";

type Props = {};

const SectionTitle = (props: Props) => {
  return (
    <div className="h-[60px] w-full flex justify-center items-center text-xl font-bold ">
      Our Services
    </div>
  );
};

export default SectionTitle;
